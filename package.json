{
  "name": "crisis-response-app-india",
  "version": "1.0.0",
  "description": "Real-Time Disaster Response App (India) with Geo-triage, Crash Feed and Collision Detection",
  "main": "server/server.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client --legacy-peer-deps",
    "server": "npm run start --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
